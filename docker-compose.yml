services:
  coldstorage:
    build: .
    container_name: coldstorage
    ports:
      - "9000:9000"
    volumes:
      - coldstorage-data:/app/data
    restart: unless-stopped

volumes:
  coldstorage-data:
